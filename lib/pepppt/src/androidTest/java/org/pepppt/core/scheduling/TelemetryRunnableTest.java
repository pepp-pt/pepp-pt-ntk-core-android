package org.pepppt.core.scheduling;

import android.util.Log;

import org.junit.Test;
import org.pepppt.core.ble.ScannerCallback;
import org.pepppt.core.util.Units;

import static org.junit.Assert.*;

public class TelemetryRunnableTest {
/*
    private long started;

    public long getSecondsSinceStart() {
        return (System.currentTimeMillis() - started + 20 * Units.MINUTES)/1000;
    }

    @Test
    public void exceptionTest() {
        started = System.currentTimeMillis();
        float value = (float) ((float)getSecondsSinceStart() /60.0);
        Log.i("TelemetryRunnableTest", String.valueOf(value));
    }


 */
}